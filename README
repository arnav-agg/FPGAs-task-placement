ResourceUsage.csv: contain relavent resource usage data for each benchmark; column 1-3 correspond to LogicUtilization, RAMBlocks and Frequency, respectively.

p1.csv - p4.csv: contain peak temperature (column1) and peak power(column2) for each benchmark.

data_processing.py: generate training data and use cross-validation to find the best parameters for each model

task_placement.py: use the trained model to do dependent task placement and get the accuracy

task_placement_independent.py: use the trained model to do independent task placement and get the accuracy
